/**
 * Marker for the framework-independent domain boundary.
 *
 * Product rules, seeded randomness and gameplay behavior are intentionally absent
 * until their open decisions and feature work are authorized.
 */
export const GAME_CORE_BOUNDARY = 'framework-independent' as const
